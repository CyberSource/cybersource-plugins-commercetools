{
  "destination": {
    "type": "HTTP",
    "url": "${apiEndpointBaseUrl}/api/extension/payment/update",
    "authentication": {
      "type": "AuthorizationHeader",
      "headerValue": "Basic ${apiEndpointAuthorization}"
    }
  },
  "key": "isv_payment_update_extension",
  "timeoutInMs": 10000,
  "triggers": [
    {
      "actions": [
        "Update"
      ],
      "resourceTypeId": "payment"
    }
  ]
}
